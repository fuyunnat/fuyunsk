interface:
  display_name: "Production Engineering"
  short_description: "Opt-in production engineering workflow"
  default_prompt: "Use $production-engineering for this real-project task, load only the references required, verify the result, and report briefly."
policy:
  allow_implicit_invocation: false
