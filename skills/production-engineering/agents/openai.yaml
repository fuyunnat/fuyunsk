interface:
  display_name: "Production Engineering"
  short_description: "Safe engineering help in plain language"
  default_prompt: "Use $production-engineering to handle this engineering task safely, verify the result, and explain every important decision in plain language."
policy:
  allow_implicit_invocation: true
