
### 文件组织原则

- 默认一个文件只承担一个主要职责。页面、组件、接口、状态、校验、持久化、工具、常量、样式分开处理。
- 如果新增功能会让一个文件同时承担两个以上职责，先拆分文件，再实现功能。
- 只有高度内聚、同改同测、同回滚的功能闭环，才允许继续放在同一文件，并且要说明为什么不拆。
- 不得为了省事把多个独立功能、多个业务概念、多个接口流程、多个页面状态塞进一个文件。
- 新文件命名和目录位置优先沿用项目现有结构，不随手发明新的层级。
