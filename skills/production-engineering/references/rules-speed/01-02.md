
### 回归保护原则

- 新增或修改功能时，必须先列出本次改动明确会影响的行为，再把不应该变化的旧行为写成禁止碰触范围。
- 默认只改当前需求相关的最小代码路径，不为了顺手修复去改无关功能。
- 如果一个变更可能影响已有功能，必须先找现有调用方、共享入口、公共状态和复用组件，确认不会误伤别的流程。
- 发现原本正常的功能有失效风险时，先停下来做只读确认，再决定是否扩大修改范围。
- 回归验证必须覆盖旧功能、边界场景和失败场景，不能只看新增功能跑通。
- 如果一个功能改动后让原本正常的行为变坏，默认视为回归失败，必须优先修复回归而不是继续叠加新改动。
