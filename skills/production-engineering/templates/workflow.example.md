# 工作流程

> 此为待填写模板，不是已执行结果。放到目标项目根目录 `工作流程.md`；已有文件时合并新任务，不覆盖历史。待做、进行中、待验证和阻塞保持未勾选；完成并验证后才改为 `[x]`。

## 任务：<唯一任务编号> — <任务名称>

项目根目录：<本次正在开发的目标项目绝对路径，不是 Codex 或技能目录>
聊天源目录依据：<当前聊天关联源文件夹的实际路径；不能由 Skill 位置推断>
清单绝对路径：<目标项目绝对路径>/工作流程.md；不得跨项目共用。
目标：<用户本次明确要求>
依据：<已读取的源码路径、版本或其他实际材料>
允许改变：<本次范围>
必须保留：<原有外观、交互、接口、数据及其他约束>
禁止操作：<不可触碰文件及未授权操作>
当前项：<事项编号>；下一步：<具体动作>

- [ ] W001【待做】<具体事项与验收标准>
- [ ] W002【待做】<具体事项与验收标准>
- [ ] W003【待做】<必要验证或已授权交付事项>

证据：<逐项填写命令与结果摘要，或安全的可核对证据路径>
阻塞：<无，或缺失条件、失败原因及恢复条件>
关联记录：<仅需要时填写任务状态/迁移台账路径，不复制完整台账>
回滚：<当前任务的恢复点与范围>

## 后续任务

新任务追加独立区块，继续旧任务更新原区块；取消须有明确依据，不当作完成。
