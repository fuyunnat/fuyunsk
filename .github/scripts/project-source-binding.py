from pathlib import Path
import sys, re
root = Path(sys.argv[1]).resolve()
def read(p): return (root / p).read_text(encoding='utf-8')
def write(p,s): (root / p).write_text(s,encoding='utf-8')
def replace(p,old,new):
    s=read(p)
    if s.count(old)!=1: raise RuntimeError(f'替换依据不唯一：{p}: {old[:60]}')
    write(p,s.replace(old,new,1))
S='skills/production-engineering/'
p='工作流程.md'
write(p,read(p).rstrip()+'''

## 任务：source-root-binding-20260906 — 聊天源目录绑定与动态清单

项目根目录：当前维护的 `fuyunnat/fuyunsk` 源码仓库；不代表其他聊天项目。
目标：工作清单绑定当前聊天项目的源文件夹；目标内发现漏项自动追加，项目之间隔离。
依据：用户本次明确要求；固定基线 `333924e4bd12e1946d09436495228d75a26d5c57`。
保留：旧任务历史、全部原稿与切片、前端保留边界、Morphicons、迁移检查、旧状态协议和批量读取。
当前项：S001；下一步：修正规则、补回归检查并验证。

- [ ] S001【进行中】绑定当前聊天源目录，区分 Skill 路径并补充动态清单规则。
- [ ] S002【待做】验证入口、模板、安装边界、动态清单契约与原有自检。
- [ ] S003【待做】提交已验证的修改并核对主线，成功后补记。

风险：本次只维护技能规则，不访问用户电脑，不证明实际模型一定遵守或本机已纠正路径。
回滚：用对应提交的反向提交撤销本次必要改动，保留后续修改和旧任务记录。
''')
p=S+'references/workflow-checklist.md'
s=read(p)
a=s.index('实现、修复、重构、迁移及已授权的工程交付，')
b=s.index('\n\n普通聊天不创建清单。',a)
s=s[:a]+'''实现、修复、重构、迁移及已授权的工程交付，在首次任务写入前，先从**当前聊天所属项目关联的源文件夹**核实正在开发的目标项目根目录，再检查项目规则、已有文件和 Git 差异。这里“源文件夹”指聊天项目设置中的源码文件夹，不是工程内部名为 `src/` 的子目录。项目名称只是显示名，不足以推断绝对路径；使用客户端实际暴露的源目录/会话工作区与真实文件交叉核对，不假装能看到未提供的设置。

把该源文件夹的实际绝对路径绑定为 `PROJECT_ROOT`，本次清单唯一写入 `PROJECT_ROOT/工作流程.md`。`SKILL_DIR` 只用于读取技能规则、模板和脚本；禁止从 `SKILL_DIR`、`__dirname`、技能脚本位置或读取技能后变化的 `cwd` 推导清单路径。即使临时 `cd` 到技能、工具或业务子目录，`PROJECT_ROOT` 不随 `cd` 改变；写入使用已核实的绝对路径并设置正确工作目录。它不是 `CODEX_HOME`、`~/.codex/`、Codex 应用目录、技能安装目录、临时下载目录或用户主目录；全局短指令只保存规则，不能把任一业务项目根目录写死成所有会话共用路径。

真实工作树根目录只是定位线索，不能机械把上级 Git 根当作当前子项目根。多项目各自记录；多个聊天关联同一实际项目时复用该项目同一清单并用独立任务区块区分，避免并发覆盖。关联多个源文件夹时先核对当前任务属于哪一个；无法消除歧义才问具体目录，不按第一个目录或显示名盲选。切换聊天项目或实际工作树时重新核对绑定，不沿用其他项目的清单、事项或完成状态。工作树与设置源目录不一致时先报告，不能在两边静默同步写入。

跨框架迁移时，“参考源码目录”和“当前聊天关联源目录”必须区分：用户明确把独立目标工程指定为当前项目根时才写在目标工程；仅新建目标工程子目录，不自动把总清单移进去。参考源码目录默认只读，原位迁移就写原项目根目录。根目录不明确先只读查证；同名目录、符号链接、来源不明的已有同名文件或写入冲突先暂停该处并说明，不能覆盖或跟随到其他项目。每次实际写入核对文件所在目录与绑定路径相同，不能把全局共享文件当作本项目清单。

发现之前写错到 Skill 或其他项目时，先停止更新错处，只读核对两边内容与任务归属。在当前项目已有写入授权内，将属于本项目的条目复制或合并回正确清单，保留已有内容；其他项目条目不得带入，原文件不删除、不清空、不覆盖。归属不明或修正需要额外权限时先确认。报告旧路径、正确路径和保留处理结果，不能只口头说已纠正。''' +s[b:]
marker='\n## 恢复与完成\n'
assert s.count(marker)==1
s=s.replace(marker,'''
## 目标模式与动态追加

给出明确实施目标即可自行拆解清单，不以用户没有预先审批整份清单为由等待；用户明确要求“先确认方案或清单再动手”时例外。目标模式不扩大权限。已开始实现却缺清单时，根据当前源码、差异与已有证据补建，保留正确成果，不重置任务，不凭聊天回忆补打勾。

读取源码、实现、联调或测试时，发现原目标内遗漏的旧功能、必要依赖、失败场景和验收步骤，先登记“发现依据、对应目标、验收方式”，再在已授权范围内继续。新发现的必做事项也属于完成条件，不因最初没列而省略；不要求每项都先让用户确认。必要步骤涉及尚未授权的高风险操作时仍须确认，不能靠加入清单自行授权。

超出原目标的建议标为待确认，不默认实施；关键歧义查证后仍存在时标阻塞并问具体问题，其他独立、明确且已授权事项继续。不得借追加事项改设计、改业务、删功能或降低验收标准。

最终对照用户最新目标、源码盘点、验收标准与更新后的清单；不只验收最初清单。涉及原貌重写时，应登记同环境新旧页面对照、交互和响应式验证；涉及后台实现时，应登记旧功能逐项验收及与真实后端联调。只有新版截图、构建通过或页面能打开不构成原貌和完整联调证据。这是按任务取适用项，不把某个项目的语言或技术栈写死成全局要求。

'''+marker,1)
write(p,s)
p=S+'SKILL.md'
s=read(p)
a=s.index('工程任务可自动调用或点名 ')
b=s.index('\n\n1. ',a)
s=s[:a]+'''工程任务可自动调用或点名 `$production-engineering`，不扩权。清单绑定当前聊天关联源文件夹的 `PROJECT_ROOT/工作流程.md`（非 Codex/技能目录），小改也记、续作先读、漏项追加、验后打勾；未授权只读不写。见 `references/workflow-checklist.md`。'''+s[b:]
s=s.replace('有 Node.js 时一次取齐：','有 Node.js 则批量读取：',1)
write(p,s)
p='global-AGENTS.example.md'
s=read(p)
lines=s.splitlines()
matches=[i for i,l in enumerate(lines) if l.startswith('- ') and '工作流程.md' in l]
assert len(matches)==1
lines[matches[0]]='- 实现清单写入当前聊天关联源目录 `PROJECT_ROOT/工作流程.md`，不是 Codex/技能目录；不随 `cd` 改变，不跨项目共用。漏项追加、验后勾选、续作先读、保留历史；小改也记，未授权只读不写。'
write(p,'\n'.join(lines)+'\n')
p='docs/personal-custom-instructions.md'
s=read(p)
s,n=re.subn(r'```markdown\n[\s\S]*?\n```','```markdown\n'+read('global-AGENTS.example.md').rstrip()+'\n```',s,count=1)
assert n==1
write(p,s)
p=S+'templates/workflow.example.md'
s=read(p)
line=next(l for l in s.splitlines() if l.startswith('项目根目录：'))
s=s.replace(line,line+'\n聊天源目录依据：<当前聊天关联源文件夹的实际路径；不能由 Skill 位置推断>\n清单绝对路径：<目标项目绝对路径>/工作流程.md；不得跨项目共用。',1)
write(p,s)
p='README.md'
replace(p,'## 项目根目录工作流程\n','''## 项目根目录工作流程

**以当前聊天项目关联的源文件夹为准，不是 Skill 所在目录。** 先核实该文件夹的绝对路径并绑定 `PROJECT_ROOT`，清单只写 `PROJECT_ROOT/工作流程.md`。这里不是内部 `src/` 子目录，也不是临时终端目录；读取技能或 `cd` 到工具目录不改变清单位置。多个项目各自保管，不共用一份总表；关联多个源目录但归属不明时，只问具体目录。

本仓库根目录的 `工作流程.md` 只记录 fuyunsk 自身维护，不是安装后的业务清单。它不应复制到技能安装目录并作为各项目共同进度；安装仅包含 `skills/production-engineering/`，其中 `templates/workflow.example.md` 是空白格式，不是实际任务。

直接给目标即可由 AI 建立并持续补全清单，不必先逐项审批；明确要求先确认时例外。目标内的旧功能、联调、失败场景及验收漏项先追加依据再推进；范围外内容和缺失授权另行确认，不能凭初版清单勾完就宣布完成。已经开工时补建清单，不清空或重做已有正确成果。
''')
p='docs/ai-installation.md'
s=read(p)
s+='''

### 防止把技能目录当成聊天项目

`SKILL_DIR` 是规则安装位置，`PROJECT_ROOT` 是当前聊天项目关联源文件夹的已核实绝对路径；二者用途不同。安装和维护脚本可能改变 `cwd`，不能把变化后的目录当业务项目。清单只写 `PROJECT_ROOT/工作流程.md`；全局模板不能填入一个固定业务根目录供所有项目共用。

不要把本技能仓库根目录的 `工作流程.md` 复制到技能安装目录作共享清单；安装仅复制 `skills/production-engineering/`。已有错放文件只读核对，不在升级时自动删除、搬移或合并未知业务历史。业务会话确认归属和写入授权后再合并到正确项目。

行为验收须在获准的两个隔离测试项目分别创建清单：项目甲的任务不得出现在项目乙；临时从项目目录切到技能目录读取规则，再继续项目甲，仍应写回项目甲。只读会话不写；没有源目录信息或多目录归属不明时先问。不把静态文案测试说成已实测这些行为。目标模式还应试验执行中新增原目标内漏项、未验证不打勾，记录未完成项而非重置清单。
'''
write(p,s)
p=S+'references/context-memory-continuity.md'
write(p,read(p).rstrip()+'''

## 聊天项目绑定不能被工具路径覆盖

根目录可见清单以当前聊天关联源文件夹的 `PROJECT_ROOT/工作流程.md` 为准，恢复先核对项目身份和实际路径。旧状态助手的注册表、Git 根定位和技能 `__dirname` 都不是这份清单的路径来源；其旧状态协议保持兼容，但不得借用其项目外存储来替代用户要求的可见清单。切到另一聊天项目时重新核对，不串用任务或勾选状态；同一目标中发现的漏项持续追加，不等待用户预先审批整表。
''')
p='tests/workflow-checklist.test.js'
s=read(p)
s+='''

// 这些是执行文案与读取链路回归检查，不是模型端到端的目录隔离证明。
test('清单根绑定当前聊天源文件夹，显示名和 src 子目录都不是绝对路径', () => {
  requireText(rules(), ['当前聊天所属项目关联的源文件夹', '项目名称只是显示名', '不是工程内部名为 `src/`', 'PROJECT_ROOT/工作流程.md']);
});
test('读取技能改变 cwd 不能改变业务清单根，不在全局模板锁死业务路径', () => {
  requireText(rules(), ['SKILL_DIR', '__dirname', 'PROJECT_ROOT` 不随 `cd` 改变', '不能把任一业务项目根目录写死']);
  const entry = text('skills/production-engineering/SKILL.md');
  const bridge = text('global-AGENTS.example.md');
  requireText(entry, ['当前聊天关联源文件夹', 'PROJECT_ROOT/工作流程.md']);
  requireText(bridge, ['当前聊天关联源目录', '不跨项目共用']);
});
test('多源目录和多聊天的清单隔离必须核对实际归属', () => {
  requireText(rules(), ['多个源文件夹', '不按第一个目录或显示名盲选', '重新核对绑定', '同一实际项目', '独立任务区块', '不自动把总清单移进去']);
});
test('错放清单只合并本项目记录，不删除原记录或带入其他项目', () => {
  requireText(rules(), ['先停止更新错处', '其他项目条目不得带入', '原文件不删除、不清空、不覆盖', '报告旧路径、正确路径']);
});
test('目标模式没有预先审批清单也要拆解，开始后补建不重置成果', () => {
  requireText(rules(), ['不以用户没有预先审批整份清单为由等待', '先确认方案或清单再动手', '保留正确成果，不重置任务', '不凭聊天回忆补打勾']);
});
test('中途漏项纳入完成条件但不能自行扩权，独立明确工作继续', () => {
  requireText(rules(), ['发现依据、对应目标、验收方式', '新发现的必做事项也属于完成条件', '不能靠加入清单自行授权', '其他独立、明确且已授权事项继续', '不只验收最初清单']);
});
test('安装区分维护历史与业务清单并要求两个项目实际验证', () => {
  requireText(text('docs/ai-installation.md'), ['不要把本技能仓库根目录的 `工作流程.md` 复制到技能安装目录', '项目甲的任务不得出现在项目乙', '不把静态文案测试说成已实测']);
  requireText(text('README.md'), ['只记录 fuyunsk 自身维护', '不必先逐项审批']);
});
test('同批读取携带目录隔离与动态清单而不改变原稿接口', () => {
  const out = reader.readBatch(reader.parse(['--stage', '实现', '--topic', '前端,整项目迁移']));
  const files = out.references.filter(e => e.file === 'workflow-checklist.md');
  assert.equal(files.length, 1);
  requireText(files[0].text, ['PROJECT_ROOT/工作流程.md', '目标模式与动态追加', '新旧页面对照', '真实后端联调']);
});
'''
write(p,s)
p='工作流程.md'
s=read(p).replace('当前项：S001；下一步：修正规则、补回归检查并验证。','当前项：S002；下一步：运行完整自检并核对差异。')
s=s.replace('- [ ] S001【进行中】绑定当前聊天源目录，区分 Skill 路径并补充动态清单规则。','- [x] S001【已完成】绑定当前聊天源目录，区分 Skill 路径并补充动态清单规则；证据：本次修改的入口、工作清单说明、模板及安装说明已逐项核对。')
s=s.replace('- [ ] S002【待做】','- [ ] S002【进行中】')
write(p,s)
assert len(read(S+'SKILL.md').encode()) <= 6000, '入口超出原体积预算'
assert len(read('global-AGENTS.example.md').encode()) <= 1800, '全局短模板超出原体积预算'
print('已更新十个相关文件；未更改原稿、切片、运行脚本或业务项目。')
