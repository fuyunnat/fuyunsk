from pathlib import Path
import sys
p = Path(sys.argv[1]).resolve() / 'skills/production-engineering/references/workflow-checklist.md'
old = p.read_text(encoding='utf-8')
assert '目标模式与动态追加' in old and 'PROJECT_ROOT/工作流程.md' in old
content = '''# 根目录工作流程清单

实现任务在项目根目录维护 `工作流程.md`，做完一项、验证一项、打勾一项。它是用户可见的唯一任务清单，不是聊天临时计划或 `.github/workflows`。本规则定向补充原文第二章续航：小改也记简表，不因此升级完整通道或强制运行状态助手。

## 位置与适用范围

首次写入前，从**当前聊天所属项目关联的源文件夹**核实正在开发的目标项目根目录，检查项目规则、已有文件和 Git 差异。源文件夹不是工程内部名为 `src/` 的子目录；项目名称只是显示名，不能推出绝对路径。只用实际暴露的会话源目录、工作区与文件查证，不能假装看过未提供的设置。

绑定该目录的绝对路径为 `PROJECT_ROOT`，清单只写 `PROJECT_ROOT/工作流程.md`。`SKILL_DIR` 只供读取规则、脚本和空白模板；禁止用它、`__dirname`、脚本位置或临时 `cwd` 推导清单路径。`PROJECT_ROOT` 不随 `cd` 改变；每次写入核对绝对路径和工作目录。不是 `CODEX_HOME`、`~/.codex/`、Codex 应用、技能安装、下载或用户主目录；全局规则不能把任一业务项目根目录写死供所有聊天共用。

真实工作树根目录只是定位线索，不能机械把上级 Git 根当作当前子项目根。多项目各自记录；同一实际项目的多个聊天用同一清单的独立任务区块，协调写入防覆盖。多个源文件夹先查任务归属，不按第一个目录或显示名盲选；仍不明才问。切项目或工作树重新核对绑定，不串用任务和完成状态；工作树与设置源目录不符时先报告，不在两处静默同步。

只有用户明确将独立目标工程指定为当前根时才写在目标工程；仅新建工程子目录不自动把总清单移进去。参考源码目录默认只读，原位迁移就写原项目根目录。根不明、同名目录、符号链接、未知同名文件或写入冲突先只读查证，不能覆盖或跟随到其他项目。

已有清单先读再合并，保留用户说明和历史；旧记录先核对再建立入口，不自动搬移删除。换名换址须用户明确要求，不能静默退回隐藏目录。无写权限时报告实际阻塞，不冒充已写入。错放时先停止更新错处，只合并已查明且获准写入的本项目记录；其他项目条目不得带入，原文件不删除、不清空、不覆盖。归属或权限不明先问，报告旧路径、正确路径及处理结果。

普通聊天不创建清单；只读、先讨论、不要改文件时不创建或更新，可读相关已有记录。仅授权写清单时只写该文档，不能由此修改业务代码。安装只提供规则与模板，不给所有项目批量建清单，不将技能仓库维护历史当业务进度。

## 一项一个状态

普通 Markdown 复选框加中文状态，稳定编号；以下只是格式示例：

```markdown
- [ ] W001【待做】尚未开始的事项。
- [ ] W002【进行中】正在处理的事项；下一步：具体动作。
- [ ] W003【待验证】实现已写完，必要验证未通过。
- [ ] W004【阻塞】缺条件或验证失败；原因和恢复条件：具体说明。
- [x] W005【已完成】实际完成且适用验证通过；证据：命令、结果或文件位置。
- [ ] W006【已取消】用户明确取消；依据：确认来源；不计为完成。
```

先列必做事项和验收，不复制全套规范，不写无法验收的“完善一下”。开始前标进行中，做完立即更新该项，不能等全部做完才回填；未测为待验证，失败为阻塞，验证通过才打勾。调研以实际源码/文档证据验收，不强制跑测试；同一验证覆盖多项可共享证据，不重复执行。父项必须等适用子项均通过，迁移共存、废弃和完成仍守原检查。

默认推进一个逻辑事项；独立并行须明确负责方与工作区，由单一协调者合并清单。新任务追加标题与任务编号；续作更新原区块，不重编号、不重置未完成项、不删失败和取消记录。源码或验收变化使证据过期时重开受影响项，修复并重验后才重勾。

## 目标模式与动态追加

明确实施目标即可自行拆解，不以用户没有预先审批整份清单为由等待；明确要求“先确认方案或清单再动手”时例外。已开工缺清单时按真实源码、差异和证据补建，保留正确成果，不重置任务，不凭聊天回忆补打勾。

明确的新增需求继续追加；读源码、实现、联调和测试发现目标内旧功能、依赖、失败场景或验收漏项，先记发现依据、对应目标、验收方式，再按依赖和已有授权推进。新发现的必做事项也属于完成条件，无需逐项审批；不能靠加入清单自行授权，高风险缺授权仍须问。

范围外建议待确认；关键歧义查证后仍影响结果则标阻塞提问，其他独立、明确且已授权事项继续。不得借追加改设计、业务、删功能或降低验收。目标模式不扩大权限。

最终对照最新目标、源码盘点、验收与动态清单，不只验收最初清单。原貌迁移须登记同环境新旧页面对照、交互及响应式验证；后台须登记旧功能验收和真实后端联调。新版截图、构建成功或页面打开不能替代这些证据；只登记适用项，不将某项目技术栈变成全局要求。

## 恢复与完成

当前区块保留项目根目录、目标、源码基线、允许改变与必须保留项、禁止操作、当前项、下一步、阻塞和证据路径；长任务决定与验收不压成模糊一句话。每次继续、换会话、上下文压缩或问进度，先读根目录当前任务区块，再查相关源码、Git 差异和证据；不能只凭聊天回忆或把历史勾选当新任务完成。

记录与事实冲突先纠正。当前目标有待做、进行中、待验证、阻塞或未经确认的取消项，不得报整个任务完成；历史或其他任务的未完项不错误阻塞本任务。交付报告给出清单实际路径与未完项，不拿百分比替代验收。

## 与现有证据机制协作

根目录清单管待办和约束，旧 `task-state.js` 管指纹与凭据，迁移台账管旧功能及验收；后两者按原条件使用，清单只记路径、任务编号及必要摘要，不维护三份互相独立的待办。旧状态指向同一清单区块，旧命令和文件兼容，不擅自覆盖迁走。

清单打勾不替代 `check` / `finalize`，也不自动调用程序。旧指纹可能包含清单：记录真实业务验证、更新清单后再查文档差异；修改清单后旧指纹失效须核实，不手填成功或排除文件绕过。仅进度变更可先确认源码未变、原证据仍适用，再查该文档，不为一个勾重跑全部测试；源码、配置、测试或环境改变仍按影响重验。核验后不无意义刷新时间戳。

清单是项目交付文档；当前任务获准提交/推送才随相关改动精准交付，不自动授权远端。明确不提交则保留本地并说明，不为了隐藏它改 `.gitignore`。不写密钥、个人数据、完整日志或敏感配置，公开前检查。

提交/推送成功且引用复核后才勾选，不能预先声明成功或虚构未来 SHA。可用后续文档提交回填，也可保留待同步并如实报告；不能为记录自身无限递归提交。

## 保持轻量

小改通常一到三项，状态变化才更新；完成项和下一项可一次保存。当前区块与规则批量读取，不每轮全仓扫描、轮询、启动后台服务或另起模型调用。历史按区块读，归档先保留入口并确认，不自动丢弃。

空白模板 `templates/workflow.example.md` 必须填入真实任务，不复制本技能仓库维护历史。清单是执行规则与记录，不是自动发现全部功能或保证模型不遗漏的程序。
'''
assert len(content.encode('utf-8')) <= 8500
p.write_text(content, encoding='utf-8')
log = Path(sys.argv[1]).resolve() / '工作流程.md'
s = log.read_text(encoding='utf-8')
s = s.replace('当前项：S002；下一步：运行完整自检并核对差异。', '当前项：S002；下一步：首次校验因说明超体积预算失败，已合并重复表述，保留原门槛后重新验证。')
log.write_text(s, encoding='utf-8')
print(f'工作清单说明已精简为 {len(content.encode())} 字节；原 8500 字节预算保持不变。')
